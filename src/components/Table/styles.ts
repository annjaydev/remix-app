import colors from '@/helpers/theme/colors';

const styles = {
  actionIcon: {
    cursor: 'pointer'
  },
  tableContainer: {
    border: `1px solid ${colors.border.light}`,
    minWudth: '1000px',
    overflowX: 'auto'
  }
};

export default styles;
