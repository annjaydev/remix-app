import colors from '@/helpers/theme/colors';

const styles = {
  actionButton: {
    border: `1px solid ${colors.border.light}`,
    fontSize: 18,
    width: 170
  },
  dialog: {
    margin: 'auto',
    width: '450px'
  }
};

export default styles;
